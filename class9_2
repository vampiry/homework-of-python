f = open('input.fa')
t = open('codon.txt')

codon = {}

for aa in t:
    table = aa.split("=")
    codon[table[0]]=table[1:]
    
codon2 = {}
for i in codon:
    codon2[codon[(Base1[i])+(Base2[i])+(Base3[i])]]= codon[AAs[i]]
    print(codon2)

#for seq in f(0:len(seq):3):
#    print(codon[seq])
